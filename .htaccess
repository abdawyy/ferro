<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send all web requests into Laravel's public/ folder (Hostinger docroot = project root).
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
